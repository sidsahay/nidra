# nidra
Rendering engine with both realtime and offline rendering

Currently, this is in a very prototypical state, it suppports very limited features and builds only on Linux
with specific dependencies.